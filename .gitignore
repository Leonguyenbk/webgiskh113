# Mã khôi phục 2 lớp / recovery codes — KHÔNG đẩy lên git
recovery-codes.txt

__pycache__/
*.pyc
.venv/

backend/.env
backend/.venv/
backend/venv/
backend/__pycache__/
backend/**/__pycache__/
backend/data/*.geojson
*.gml
frontend/.env
frontend/node_modules/
frontend/dist/
sync_gcn/.env
sync_gcn/service_account.json
sync_gcn/venv/
sync_gcn/__pycache__/

# Chạy local qua NSSM (Windows service) + Cloudflare Tunnel — log runtime,
# không phải mã nguồn.
backend/service-std*.log
frontend/service-std*.log
tunnel-std*.log

# Thư mục tạm dùng để cài PostgreSQL/PostGIS + bản dump di chuyển dữ liệu
# từ Supabase — không đưa lên git (dump chứa dữ liệu thật, dung lượng lớn).
.setup_tmp/

# Bản export zone DNS dùng để đối chiếu lúc chuyển domain — không phải mã
# nguồn, không cần theo dõi trong git.
kh2959bmt.net.txt

